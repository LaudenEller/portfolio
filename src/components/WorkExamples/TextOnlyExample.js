import React from 'react';
import styles from './WorkExamples.module.css';

const TextOnlyExample = () => (
  <div className={styles.textOnly}>
    <h4>Text Only Example</h4>
    <p>This example shows a simple text-only layout.</p>
  </div>
);

export default TextOnlyExample;
